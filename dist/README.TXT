To run the project from the command line, go to the dist folder and
type the following:

java -jar "satNOGS-DB.jar" 

This will produce, in your current directory, 3 csv files that will contain the DB content of the satNOGS site for modes, transmitters and satellites.

